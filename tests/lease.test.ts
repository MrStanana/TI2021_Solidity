// import { artifacts, contract, it, assert } from 'truffle';

// const Lease = artifacts.require('Lease');

// contract('Lease', () => {
//   it('should create new contract', () => Lease.deployed()
//     .then((instance) => instance.identifier.call())
//     .then((identifier) => assert.equal(identifier.valueOf(), 'id')));
// });
