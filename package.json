{
  "scripts": {
    "compile": "truffle compile",
    "test": "truffle test --compile-all-debug --show-events --stacktrace ./tests/*"
  },
  "dependencies": {
    "soljs": "^6.0.0",
    "truffle": "^5.3.3",
    "web3": "^1.3.5"
  }
}
