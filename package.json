{
  "scripts": {
    "compile": "solcjs --bin --base-path contracts --output-dir build contracts/*",
    "compile:truffle": "truffle compile",
    "test": "truffle test --compile-all-debug --show-events --stacktrace ./tests/*",
    "gas": "solc contracts/* --gas"
  },
  "dependencies": {
    "solc": "^0.8.4",
    "truffle": "^5.3.4",
    "web3": "^1.3.5"
  }
}
