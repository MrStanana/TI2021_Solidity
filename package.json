{
  "scripts": {
    "compile": "truffle compile",
    "test": "truffle test --compile-all-debug --verbose-rpc --show-events"
  },
  "dependencies": {
    "soljs": "^6.0.0",
    "truffle": "^5.3.3",
    "web3": "^1.3.5"
  }
}
